{
  "scripts": {
    "start": "parcel serve src/index.pug --no-autoinstall",
    "build": "parcel build src/index.pug --no-autoinstall"
  },
  "devDependencies": {
    "parcel": "^2.0.0-beta.1",
    "pug": "^3.0.0",
    "sass": "^1.26.8"
  },
  "dependencies": {
    "reset-css": "^5.0.1"
  }
}
